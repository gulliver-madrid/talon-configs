name <user.name>:
    # 'name' is an alternative to 'word' or 'phrase' for common words
    # that are often interpreted wrongly
    "{name}"

save all:
    edit.save_all()

say california:
    app.notify('California')
